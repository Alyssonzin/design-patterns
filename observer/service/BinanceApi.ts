export class BinanceApi {

  public getPrice(): number {
    // Faz uma chamada a api da Binance para obter o preço atual do Bitcoin...

    return Math.random() * 1000; // Retorna um preço simulado
  }
}